version: "2"

linters:
  default: none
  enable:
    - errcheck
    - govet
    - ineffassign
    - revive
    - staticcheck
    - style
  settings:
    custom:
      style:
        type: module
        description: Enforce custom Go style rules with the gclint style plugin.
